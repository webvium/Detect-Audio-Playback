const video = document.querySelector('video');

video.addEventListener('play', (event) => {
    WebviumIdentifierUniqueGeneratedID.play();
});

video.addEventListener('pause', (event) => {
    WebviumIdentifierUniqueGeneratedID.pause();
});
